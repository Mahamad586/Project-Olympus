name: Olympus Engine v3.0 - "The Relentless"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *' # Runs every 6 hours

jobs:
  unleash_the_kraken:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJson(needs.read_targets.outputs.targets_json) }}
    needs: read_targets
    steps:
      - name: Checkout Olympus Codebase
        uses: actions/checkout@v4

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: pip install requests

      - name: 🚀 [Module] Reconnaissance
        run: |
          chmod +x modules/recon/run.sh
          ./modules/recon/run.sh ${{ matrix.target }}

      - name: 👁️ [Module] Vulnerability Scanning
        run: |
          chmod +x modules/vuln_scan/run.sh
          ./modules/vuln_scan/run.sh ${{ matrix.target }}

      - name: 🗣️ [Module] Analyze and Report to Discord
        if: always()
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python reporter.py ${{ matrix.target }}

  read_targets:
    runs-on: ubuntu-latest
    outputs:
      targets_json: ${{ steps.read_targets_file.outputs.targets_json }}
    steps:
      - name: Checkout Olympus Codebase
        uses: actions/checkout@v4
      - id: read_targets_file
        name: Read targets.txt into a JSON array
        run: |
          targets_json=$(jq -R -s -c 'split("\n") | map(select(length > 0))' targets.txt)
          echo "targets_json=$targets_json" >> $GITHUB_OUTPUT
